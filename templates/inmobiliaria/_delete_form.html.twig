<form method="post" action="{{ path('app_inmobiliaria_delete', {'id': inmobiliarium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inmobiliarium.id) }}">
    <button class="btn">Delete</button>
</form>
